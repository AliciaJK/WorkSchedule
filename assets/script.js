// need to display time with moment in the header 
var today = moment();
$("#currentDay").text(today.format("MMM Do, YYYY"));


// need to save data on save button click 
//need to deal with muliple boxes & saving each one (for loop)
var words = ''

$('textarea').val('Write your notes here.')
window.localStorage.setItem("textarea", words);
    console.log(value);
    return this.value.trim() //this is the save


// save on button click 
document.querySelector('textarea').addEventListener('Save', funciton(saveButton))
    var wordsInBoxes

function saveButton()
//look into if save button is clicked ...then overwrite words variable
//get item
//for each 
// this (important) 

//on save display tasks 



//need to comapre time of day and time on schedule & change box colour as per css